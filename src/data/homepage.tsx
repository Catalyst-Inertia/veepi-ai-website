// Frontend homepage copy — currently only the running-text marquee fallback.
// The CMS home page is seeded from its own copy in seed/homepage.ts; section
// copy deliberately does NOT live here, so frontend edits cannot drift the
// seeded CMS content.
export const homepageRunningText = [
  'Custom Website Development & Redesign',
  'Social Media Management',
  'Digital Marketing',
  'Mobile App Development',
  'SEO Maximization',
] as const
